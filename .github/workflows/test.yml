name: 📰 Test website rebuild trigger

on: workflow_dispatch

jobs:
  release:

    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Invoke website rebuild
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: build.yml
          repo: rust-scicomp/website
          token: ${{ secrets.PAT_TOKEN }}
          ref: refs/heads/main
